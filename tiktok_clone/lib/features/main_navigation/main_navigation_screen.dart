import 'package:flutter/material.dart';

class MainNavigatoinScreen extends StatelessWidget {
  const MainNavigatoinScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
